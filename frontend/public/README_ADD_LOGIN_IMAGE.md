Drop your login background image here

1) Place the image file you want to use for the login background at:

   frontend/public/login-hero.jpg

2) The app's CSS looks for `/login-hero.jpg` (see `frontend/src/App.css`). If you prefer PNG, update the filename in `App.css` to `/login-hero.png`.

3) IMPORTANT: ensure you have the rights to use the photo (license or ownership). Do not include watermarked stock images in production.

If you want, I can add the image file for you if you upload it here or provide a permissive URL.
